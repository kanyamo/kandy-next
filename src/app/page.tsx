import React from "react";
import GameBoard from "@/components/GameBoard";

const Home: React.FC = () => {
  return (
    <main className="p-4 h-svh w-full">
      <GameBoard />
    </main>
  );
};

export default Home;
